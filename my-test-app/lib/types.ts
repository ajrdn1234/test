export type StockInfo = {
    code: string
    currentPrice: number
    changeAmount: number
    changeRate: number
    volume: number
}

export type USStockInfo = {
    symbol: string
    currentPrice: number
    changeAmount: number
    changeRate: number
    volume: number
}